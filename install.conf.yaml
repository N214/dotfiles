- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
      #~/.oh-my-zsh:
    ~/.tmux.conf:
      #~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
